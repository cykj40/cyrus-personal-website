# Phase 4b notes

- Added **Custom MCP Server Build — Starting at $8,000** immediately after Setup & Integration and before Custom AI Agent Build in `src/components/sections/Engagements.tsx`.
- Updated the existing MCP Servers offer card in `src/components/sections/Services.tsx` to state the same `$8,000` custom-build starting price while preserving the separate `$2,500` existing-server setup price and `$750` additional-integration charge.
- Reused the existing engagement card structure and styling. No other pricing tier was changed.
- Verification: `pnpm build` passes.
